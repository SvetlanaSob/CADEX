import { createTheme } from '@mui/material'

export const mediaWidthTransform: number = 650

const theme = createTheme({
	palette: {
		primary: {
			main: '#3700FFA8',
		},
		secondary: {
			main: '#B3C7FFFF',
		},
	},
})

export default theme

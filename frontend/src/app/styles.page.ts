import styled from 'styled-components'

export const MainPageContainerSC = styled.main`
	display: grid;
	grid-template-rows: repeat(3, max-content);
	padding-top: var(--header-height);
	padding-bottom: var(--footer-height);
`
